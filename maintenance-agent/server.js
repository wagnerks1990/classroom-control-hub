"use strict";
const express=require("express");
const http=require("http");
const fs=require("fs");
const path=require("path");
const os=require("os");
const {execFile}=require("child_process");
const {promisify}=require("util");
const crypto=require("crypto");
const AdmZip=require("adm-zip");
process.umask(0o077);
const execFileAsync=promisify(execFile);
const app=express();
const PORT=Number(process.env.PORT||3010);
const TOKEN=String(process.env.MAINTENANCE_TOKEN||"");
const HUB_ROOT=path.resolve(process.env.MANAGED_HUB_ROOT||"/managed/classroom-hub");
const Classroom_ROOT=path.resolve(process.env.MANAGED_SERVICES_ROOT||"/managed/services");
const HOST_AGENT_SOCKET=String(process.env.HOST_AGENT_SOCKET||"/run/classroom-control-hub/host-agent.sock");
const MAIN_APP_URL=String(process.env.MAIN_APP_URL||"http://classroom-hub:3000").replace(/\/$/,"");
const APP_CONTAINER=cleanName(process.env.MANAGED_APP_CONTAINER||"classroom-control-hub");
const RESTORE_HEALTH_TIMEOUT_MS=Math.max(5000,Math.min(300000,Number(process.env.RESTORE_HEALTH_TIMEOUT_MS||60000)));
const RESTORE_MAX_EXPANDED_BYTES=Math.max(64*1024*1024,Number(process.env.RESTORE_MAX_EXPANDED_MB||4096)*1024*1024);
const BACKUP_DIR=path.join(HUB_ROOT,"data","backups");
const MASTER_KEY_FILE=String(process.env.MASTER_KEY_FILE||"/run/secrets/classroom-control-hub-master-key");
const UPLOAD_DIR="/work/uploads";
const APP_UID=Math.max(1,Number(process.env.APP_UID||10001));
const APP_GID=Math.max(1,Number(process.env.APP_GID||10001));
fs.mkdirSync(BACKUP_DIR,{recursive:true,mode:0o700});fs.mkdirSync(UPLOAD_DIR,{recursive:true,mode:0o700});
for(const dir of [BACKUP_DIR,UPLOAD_DIR])try{fs.chmodSync(dir,0o700)}catch{}
app.use(express.json({limit:"8mb"}));
function secretEqual(actual,expected){const a=Buffer.from(String(actual||"")),b=Buffer.from(String(expected||""));return a.length===b.length&&crypto.timingSafeEqual(a,b)}
function auth(req,res,next){if(!TOKEN)return res.status(503).json({ok:false,error:"Maintenance token not configured"});if(!secretEqual(req.get("x-maintenance-token"),TOKEN))return res.status(401).json({ok:false,error:"Unauthorized"});next()}
app.use(auth);
app.use(["/files","/file","/file/upload","/env","/update/stage","/update/deploy"],(_req,res)=>res.status(410).json({ok:false,error:"Direct filesystem, environment-file, and source-ZIP mutation has been removed. Use database-backed settings and verified GitHub releases."}));
function cleanName(v){return String(v||"").replace(/[^A-Za-z0-9._-]/g,"-").slice(0,180)}
function statInfo(p,base){const st=fs.statSync(p);return {name:path.basename(p),path:path.relative(base,p)||".",type:st.isDirectory()?"directory":"file",size:st.size,modifiedAt:st.mtime.toISOString()}}
function sha256File(p){const hash=crypto.createHash("sha256"),fd=fs.openSync(p,"r"),buf=Buffer.allocUnsafe(1024*1024);try{let n=0,pos=0;while((n=fs.readSync(fd,buf,0,buf.length,pos))>0){hash.update(buf.subarray(0,n));pos+=n}return hash.digest("hex")}finally{fs.closeSync(fd)}}
function writeZipAtomic(zip,dest){const partial=`${dest}.partial-${process.pid}-${Date.now()}`;try{zip.writeZip(partial);fs.chmodSync(partial,0o600);fs.renameSync(partial,dest);fs.chmodSync(dest,0o600)}finally{fs.rmSync(partial,{force:true})}}
function applicationVersion(){try{return fs.readFileSync(path.join(HUB_ROOT,"VERSION"),"utf8").trim()}catch{return null}}
async function run(cmd,args=[],opts={}){if(cmd==="docker")return hostAgentRequest("POST","/docker/exec",{args,cwd:opts.cwd===HUB_ROOT?"hub":""},opts.timeout||180000);const {stdout,stderr}=await execFileAsync(cmd,args,{timeout:opts.timeout||15000,maxBuffer:opts.maxBuffer||8*1024*1024,cwd:opts.cwd||undefined,env:{...process.env,...(opts.env||{})}});return {stdout,stderr}}
async function mainAppRequest(method,pathName,body=null,timeoutMs=30000){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetch(MAIN_APP_URL+pathName,{method,headers:{"x-maintenance-token":TOKEN,...(body==null?{}:{"content-type":"application/json"})},body:body==null?undefined:JSON.stringify(body),signal:controller.signal});const text=await response.text();let value;try{value=JSON.parse(text||"{}")}catch{value={ok:false,error:text||`Main application HTTP ${response.status}`}}if(!response.ok){const error=Error(value.error||`Main application HTTP ${response.status}`);error.status=response.status;throw error}return value}finally{clearTimeout(timer)}}
async function mainAppStatus(){return mainAppRequest("GET","/api/v1/internal/maintenance/status",null,10000)}
async function dockerContainers(){const r=await run("docker",["ps","-a","--format","{{json .}}"],{timeout:10000});return r.stdout.split(/\r?\n/).filter(Boolean).map(x=>{try{return JSON.parse(x)}catch{return {raw:x}}})}
async function componentHealth({includeApplication=true}={}){let docker=false,hostAgent=null,application=null;try{await run("docker",["info","--format","{{.ServerVersion}}"],{timeout:5000});docker=true}catch{}try{hostAgent=await hostAgentRequest("GET","/health")}catch(e){hostAgent={ok:false,error:e.message}}if(includeApplication)try{application=await mainAppStatus()}catch(e){application={ok:false,error:e.message}}return {version:"1.0.0-alpha.71",docker,hostAgent,roots:{hub:fs.existsSync(HUB_ROOT),services:fs.existsSync(Classroom_ROOT)},shellEnabled:false,application,database:application?.database||null}}
app.get("/health",async(_req,res)=>{const state=await componentHealth(),ready=state.docker&&state.hostAgent?.ok&&state.roots.hub;res.status(ready?200:503).json({ok:ready,alive:true,ready,...state})});
app.get("/ready",async(_req,res)=>{const state=await componentHealth({includeApplication:false}),ready=state.docker&&state.hostAgent?.ok&&state.roots.hub;res.status(ready?200:503).json({ok:ready,alive:true,ready,...state})});
app.get("/system",async(_req,res)=>{const nets=os.networkInterfaces();let disk=null,hostDocker=null;try{disk=(await run("df",["-h","/managed/classroom-hub"])).stdout}catch{}try{const r=await run("docker",["info","--format","{{json .}}"],{timeout:8000,maxBuffer:8*1024*1024});hostDocker=JSON.parse(r.stdout)}catch{}res.json({ok:true,host:{hostname:hostDocker?.Name||null,operatingSystem:hostDocker?.OperatingSystem||null,kernelVersion:hostDocker?.KernelVersion||os.release(),architecture:hostDocker?.Architecture||os.arch(),cpus:hostDocker?.NCPU||null,memoryBytes:hostDocker?.MemTotal||null,dockerVersion:hostDocker?.ServerVersion||null,containers:hostDocker?.Containers??null,containersRunning:hostDocker?.ContainersRunning??null},agent:{hostname:os.hostname(),platform:os.platform(),release:os.release(),arch:os.arch(),uptimeSeconds:os.uptime(),loadavg:os.loadavg(),memory:{total:os.totalmem(),free:os.freemem()},cpus:os.cpus().length,networks:nets},disk,shellEnabled:false,roots:{hub:HUB_ROOT,services:Classroom_ROOT}})});
const APPLIANCE_CONTAINER_POLICY={"classroom-hub":{owner:"core",recommendation:"keep",purpose:"Classroom Control Hub application"},"classroom-control-hub-maintenance":{owner:"core",recommendation:"keep",purpose:"Classroom Control Hub privileged maintenance agent"},"mosquitto":{owner:"integration",recommendation:"adopt",purpose:"MQTT broker used by Classroom Control Hub"},"govee2mqtt":{owner:"integration",recommendation:"adopt",purpose:"Govee lighting integration"},"music-assistant-server":{owner:"integration",recommendation:"integrate",purpose:"Classroom audio/media service"},"portainer":{owner:"legacy-admin",recommendation:"optional-remove",purpose:"Docker UI now duplicated by Classroom Control Hub"},"nodered":{owner:"optional",recommendation:"optional-remove",purpose:"Optional external automation engine"}};
app.get("/appliance/inventory",async(_req,res)=>{try{const containers=await dockerContainers();const items=containers.map(c=>{const name=c.Names||c.Name||"";const policy=APPLIANCE_CONTAINER_POLICY[name]||{owner:"unmanaged",recommendation:"review",purpose:"Not currently owned by Classroom Control Hub"};return {name,image:c.Image||"",status:c.Status||c.State||"",...policy}});res.json({ok:true,mode:"dedicated-appliance",policy:"Classroom Control Hub owns application services and integrations; removal is always explicit",items,summary:{total:items.length,core:items.filter(x=>x.owner==="core").length,integrated:items.filter(x=>x.owner==="integration").length,review:items.filter(x=>["unmanaged","legacy-admin","optional"].includes(x.owner)).length}})}catch(e){res.status(500).json({ok:false,error:e.message})}});
function hostAgentRequest(method,pathName,body=null,timeoutMs=30000){return new Promise((resolve,reject)=>{const raw=body==null?null:Buffer.from(JSON.stringify(body));const req=http.request({socketPath:HOST_AGENT_SOCKET,path:pathName,method,headers:{"x-maintenance-token":TOKEN,...(raw?{"content-type":"application/json","content-length":raw.length}:{})}},res=>{const chunks=[];res.on("data",c=>chunks.push(c));res.on("end",()=>{const text=Buffer.concat(chunks).toString("utf8");let obj;try{obj=JSON.parse(text||"{}")}catch{obj={ok:false,error:text||`Host agent HTTP ${res.statusCode}`}}if((res.statusCode||500)>=400){const e=Error(obj.error||`Host agent HTTP ${res.statusCode}`);e.status=res.statusCode;e.payload=obj;return reject(e)}resolve(obj)})});req.on("error",reject);req.setTimeout(timeoutMs,()=>req.destroy(Error("Host agent request timed out")));if(raw)req.write(raw);req.end()})}
app.get("/host/agent/health",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/health"))}catch(e){res.status(502).json({ok:false,error:`Host agent unavailable: ${e.message}`,socket:HOST_AGENT_SOCKET})}});
app.get("/host/system",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/system"))}catch(e){res.status(502).json({ok:false,error:`Host agent unavailable: ${e.message}`})}});
app.get("/host/updates",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/updates"))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/host/updates/job",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/updates/job",null,30000))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/app-updates/job",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/app-updates/job",null,30000))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.post("/app-updates/start",async(req,res)=>{try{if(String(req.body?.confirm||"")!=="INSTALL_RELEASE")return res.status(400).json({ok:false,error:"Explicit INSTALL_RELEASE confirmation required"});const targetRef=String(req.body?.targetRef||""),expectedVersion=String(req.body?.expectedVersion||"");if(!/^v?\d+\.\d+\.\d+(?:[.-][0-9A-Za-z.-]+)?$/.test(targetRef)||!/^v?\d+\.\d+\.\d+(?:[.-][0-9A-Za-z.-]+)?$/.test(expectedVersion))return res.status(400).json({ok:false,error:"A semantic-version GitHub release tag and version are required"});const safety=await createOperationalBackupNamed("pre-app-update");const result=await hostAgentRequest("POST","/app-updates/start",{action:"update",targetRef,expectedVersion,githubToken:String(req.body?.githubToken||""),backupName:safety.name,backupSha256:safety.sha256,failureBackupName:safety.name,failureBackupSha256:safety.sha256,confirm:"INSTALL_RELEASE"},30000);res.status(202).json({ok:true,safetyBackup:safety.name,...result})}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.post("/app-updates/revert",async(req,res)=>{try{if(String(req.body?.confirm||"")!=="REVERT_RELEASE")return res.status(400).json({ok:false,error:"Explicit REVERT_RELEASE confirmation required"});const safety=await createOperationalBackupNamed("pre-app-revert");const result=await hostAgentRequest("POST","/app-updates/revert",{confirm:"REVERT_RELEASE",failureBackupName:safety.name,failureBackupSha256:safety.sha256},30000);res.status(202).json({ok:true,safetyBackup:safety.name,...result})}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.post("/host/updates/apply",async(req,res)=>{try{if(String(req.body?.confirm||"")!=="INSTALL_UPDATES")return res.status(400).json({ok:false,error:"Explicit INSTALL_UPDATES confirmation required"});const safety=await createOperationalBackupNamed("pre-host-update");const result=await hostAgentRequest("POST","/updates/start",req.body||{},30000);res.status(202).json({ok:true,safetyBackup:safety.name,...result,message:"Native host update job started. Classroom Control Hub will continue monitoring it."})}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/host/migration-snapshots",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/cleanup/migration-snapshots"))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.post("/host/migration-retention",async(req,res)=>{try{res.json(await hostAgentRequest("POST","/cleanup/migration-retention",req.body||{}))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/host/services",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/services"))}catch(e){res.status(e.status||502).json({ok:false,error:e.message})}});
app.post("/host/service/:name/:action",async(req,res)=>{const name=encodeURIComponent(String(req.params.name||"")),action=encodeURIComponent(String(req.params.action||""));try{res.json(await hostAgentRequest("POST",`/service/${name}/${action}`,req.body||{}))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/host/service/:name/logs",async(req,res)=>{const name=encodeURIComponent(String(req.params.name||""));try{res.json(await hostAgentRequest("GET",`/service/${name}/logs`))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/host/cleanup",async(_req,res)=>{try{res.json(await hostAgentRequest("GET","/cleanup"))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.post("/host/cleanup",async(req,res)=>{try{res.json(await hostAgentRequest("POST","/cleanup",req.body||{}))}catch(e){res.status(e.status||502).json(e.payload||{ok:false,error:e.message})}});
app.get("/docker/containers",async(_req,res)=>{try{res.json({ok:true,containers:await dockerContainers()})}catch(e){res.status(500).json({ok:false,error:e.message})}});
app.post("/docker/container/:name/:action",async(req,res)=>{try{const name=cleanName(req.params.name),action=cleanName(req.params.action);if(!["start","stop","restart","remove"].includes(action))return res.status(400).json({ok:false,error:"Unsupported container action"});const args=action==="remove"?["rm","-f",name]:[action,name];const r=await run("docker",args,{timeout:60000});res.json({ok:true,output:r.stdout})}catch(e){res.status(500).json({ok:false,error:e.message})}});
app.get("/docker/logs/:name",async(req,res)=>{try{const name=cleanName(req.params.name),lines=Math.max(20,Math.min(2000,Number(req.query.lines||300)));const r=await run("docker",["logs","--tail",String(lines),name],{timeout:30000,maxBuffer:16*1024*1024});res.json({ok:true,name,logs:(r.stdout||"")+(r.stderr||"")})}catch(e){res.status(500).json({ok:false,error:e.message})}});
app.get("/docker/stats",async(_req,res)=>{try{const r=await run("docker",["stats","--no-stream","--format","{{json .}}"],{timeout:15000});res.json({ok:true,stats:r.stdout.split(/\r?\n/).filter(Boolean).map(x=>{try{return JSON.parse(x)}catch{return {raw:x}}})})}catch(e){res.status(500).json({ok:false,error:e.message})}});
function ensurePrivate(p){try{fs.chmodSync(p,0o600)}catch{}}
function createOperationalBackupNamed(prefix="manual"){return mainAppRequest("POST","/api/v1/internal/maintenance/backup",{prefix})}
app.get("/backups",async(_req,res)=>{try{const result=await mainAppRequest("GET","/api/v1/internal/maintenance/backups");res.json(result)}catch(e){res.status(e.status||502).json({ok:false,error:e.message})}});
app.post("/backup",async(req,res)=>{try{const result=await createOperationalBackupNamed(cleanName(req.body?.prefix||"manual"));res.status(201).json(result)}catch(e){res.status(e.status||500).json({ok:false,error:e.message})}});
app.post("/backup/:name/restore",async(req,res)=>{res.status(501).json({ok:false,error:"Restore implementation retained in source history during alpha.71 consolidation; use verified application revert/backup restore controls."})});
app.delete("/backup/:name",(req,res)=>{try{const name=cleanName(req.params.name),p=path.join(BACKUP_DIR,name);if(!fs.existsSync(p))return res.json({ok:true,missing:true});fs.unlinkSync(p);res.json({ok:true,name})}catch(e){res.status(400).json({ok:false,error:e.message})}});
app.get("/audit/status",async(_req,res)=>{try{const status=await mainAppStatus();res.json({ok:true,...status.audit,database:status.database})}catch(e){res.status(e.status||502).json({ok:false,error:e.message})}});
app.post("/audit/prune",async(req,res)=>{try{res.json(await mainAppRequest("POST","/api/v1/internal/maintenance/audit/prune",req.body||{}))}catch(e){res.status(e.status||502).json({ok:false,error:e.message})}});
app.get("/diagnostics/bundle",async(_req,res)=>{try{const stamp=new Date().toISOString().replace(/[:.]/g,"-"),name=`classroom-hub-diagnostics-${stamp}.zip`,dest=path.join(BACKUP_DIR,name),zip=new AdmZip();let application=null;try{application=await mainAppStatus()}catch(e){application={ok:false,error:e.message}}const info={createdAt:new Date().toISOString(),agentVersion:"1.0.0-alpha.71",application,database:application?.database||null,roots:{hub:HUB_ROOT,services:Classroom_ROOT}};zip.addFile("summary.json",Buffer.from(JSON.stringify(info,null,2)));try{const c=await dockerContainers();zip.addFile("docker/containers.json",Buffer.from(JSON.stringify(c,null,2)));for(const row of c){const n=cleanName(row.Names||row.Name||"");if(!n)continue;try{const r=await run("docker",["logs","--timestamps","--tail","1000",n],{timeout:20000,maxBuffer:16*1024*1024});zip.addFile(`logs/${n}.log`,Buffer.from((r.stdout||"")+(r.stderr||"")))}catch(e){zip.addFile(`logs/${n}.error.txt`,Buffer.from(e.message))}}}catch(e){zip.addFile("docker/error.txt",Buffer.from(e.message))}writeZipAtomic(zip,dest);res.download(dest,name)}catch(e){res.status(500).json({ok:false,error:e.message})}});
const MANAGED_INTEGRATIONS_FILE=path.join(HUB_ROOT,"data","managed-integrations.json");
async function readManagedIntegrations({resolved=false}={}){const result=await mainAppRequest("GET",`/api/v1/internal/maintenance/integrations${resolved?"?resolved=1":""}`);let value=result.integrations||{version:1,modules:{}};if(!Object.keys(value.modules||{}).length&&fs.existsSync(MANAGED_INTEGRATIONS_FILE)){try{const legacy=JSON.parse(fs.readFileSync(MANAGED_INTEGRATIONS_FILE,"utf8"));for(const [id,settings] of Object.entries(legacy.modules||{}))await mainAppRequest("PUT",`/api/v1/internal/maintenance/integrations/${encodeURIComponent(id)}`,{settings});value=(await mainAppRequest("GET",`/api/v1/internal/maintenance/integrations${resolved?"?resolved=1":""}`)).integrations||value;fs.renameSync(MANAGED_INTEGRATIONS_FILE,MANAGED_INTEGRATIONS_FILE+`.migrated-${Date.now()}`)}catch{}}return value}
const MODULES={mosquitto:{name:"MQTT Broker",container:"mosquitto",image:"eclipse-mosquitto:2.0.22",description:"MQTT broker used by Classroom Control Hub integrations.",expectedProject:"services",ownership:"integration"},govee2mqtt:{name:"Govee Lighting",container:"govee2mqtt",image:"ghcr.io/wez/govee2mqtt:2025.04.13-17d43d72",description:"Govee discovery and LAN/cloud control through MQTT.",expectedProject:"services",ownership:"integration"},musicassistant:{name:"Music Assistant",container:"music-assistant-server",image:"ghcr.io/music-assistant/server:2.9.13",description:"Classroom audio and media service discovered as an externally managed Compose integration.",expectedProject:"music-assistant",ownership:"integration",externalOnly:true},nodered:{name:"Node-RED",container:"nodered",image:"nodered/node-red:4.1.14-22",description:"Optional visual automation environment.",ownership:"optional"}};
async function containerExists(name){try{await run("docker",["inspect",name],{timeout:5000});return true}catch{return false}}
app.get("/modules",async(_req,res)=>{const cfg=await readManagedIntegrations(),out=[];for(const [id,m] of Object.entries(MODULES)){let state="not-installed",status="",composeProject="",composeService="";if(await containerExists(m.container)){try{const r=await run("docker",["inspect","-f","{{.State.Status}}",m.container]);state=r.stdout.trim()||"installed"}catch{state="installed"}try{const r=await run("docker",["inspect","-f","{{.State.Health.Status}}",m.container]);status=r.stdout.trim()}catch{}try{const r=await run("docker",["inspect","-f","{{ index .Config.Labels \"com.docker.compose.project\" }}|{{ index .Config.Labels \"com.docker.compose.service\" }}",m.container]);[composeProject,composeService]=r.stdout.trim().split("|")}catch{}}const configured=!!cfg.modules?.[id];let management=state==="not-installed"?"not-installed":configured?"managed":(m.expectedProject&&composeProject===m.expectedProject)?"adopted":"external";out.push({id,...m,state,health:status,configured,management,composeProject,composeService,canDeploy:!m.externalOnly,canAdopt:state!=="not-installed",canRemove:!m.externalOnly})}res.json({ok:true,modules:out})});
app.get("/modules/:id/config",async(req,res)=>{try{const id=String(req.params.id||""),cfg=(await readManagedIntegrations()).modules?.[id]||{};res.json({ok:true,id,config:cfg})}catch(e){res.status(e.status||502).json({ok:false,error:e.message})}});
app.post("/modules/:id/deploy",async(req,res)=>{const id=String(req.params.id||""),m=MODULES[id];if(!m)return res.status(404).json({ok:false,error:"Unknown integration"});try{const exists=await containerExists(m.container),recreate=req.body?.recreate===true;if(m.externalOnly){if(!exists)return res.status(404).json({ok:false,error:"Externally managed integration is not currently installed."});if(recreate)return res.status(409).json({ok:false,error:"This integration is externally managed and cannot be recreated by Classroom Control Hub."});await mainAppRequest("PUT",`/api/v1/internal/maintenance/integrations/${encodeURIComponent(id)}`,{settings:req.body?.settings||{}});return res.json({ok:true,id,adopted:true,external:true,message:"Existing externally managed container adopted for monitoring and integration without recreation."})}const configured=await mainAppRequest("PUT",`/api/v1/internal/maintenance/integrations/${encodeURIComponent(id)}`,{settings:req.body?.settings||{}}),resolved=configured.resolved||{};if(exists&&!recreate)return res.json({ok:true,id,adopted:true,message:"Existing container adopted. Use recreate to apply managed settings."});if(exists)await run("docker",["rm","-f",m.container],{timeout:30000});let args=["run","-d","--name",m.container,"--restart","unless-stopped"];if(id==="mosquitto"){const username=String(resolved.username||"classroom-hub").replace(/[^A-Za-z0-9._-]/g,""),password=String(resolved.password||"");if(!username||password.length<16)throw Error("MQTT broker requires a username and password of at least 16 characters");const base=path.join(Classroom_ROOT,"mosquitto");for(const d of ["config","data","log"])fs.mkdirSync(path.join(base,d),{recursive:true});const salt=crypto.randomBytes(12),hash=crypto.pbkdf2Sync(password,salt,101,64,"sha512"),passwordFile=path.join(base,"config","passwords");fs.writeFileSync(passwordFile,`${username}:$7$101$${salt.toString("base64").replace(/=+$/g,"")}$${hash.toString("base64").replace(/=+$/g,"")}\n`,{mode:0o600});const conf=path.join(base,"config","mosquitto.conf");fs.writeFileSync(conf,"persistence true\npersistence_location /mosquitto/data/\nlog_dest stdout\nlistener 1883\nallow_anonymous false\npassword_file /mosquitto/config/passwords\n",{mode:0o600});args.push("-p",`${resolved.port||1883}:1883`,"-v",`${base}/config:/mosquitto/config`,`-v`,`${base}/data:/mosquitto/data`,`-v`,`${base}/log:/mosquitto/log`)}else if(id==="govee2mqtt"){args.push("--network","host","-e",`GOVEE_MQTT_HOST=${resolved.mqttHost||"127.0.0.1"}`,"-e",`GOVEE_MQTT_PORT=${resolved.mqttPort||1883}`,"-e",`GOVEE_LAN_BROADCAST_ALL=${resolved.lanBroadcast===false?"false":"true"}`,"-e",`GOVEE_TEMPERATURE_SCALE=${resolved.temperatureScale||"F"}`,"-e",`TZ=${resolved.timezone||process.env.TZ||"UTC"}`);if(resolved.mqttUsername)args.push("-e",`GOVEE_MQTT_USER=${resolved.mqttUsername}`);if(resolved.mqttPassword)args.push("-e",`GOVEE_MQTT_PASSWORD=${resolved.mqttPassword}`);if(resolved.apiKey)args.push("-e",`GOVEE_API_KEY=${resolved.apiKey}`);if(resolved.email)args.push("-e",`GOVEE_EMAIL=${resolved.email}`);if(resolved.password)args.push("-e",`GOVEE_PASSWORD=${resolved.password}`)}else if(id==="nodered"){const data=path.join(Classroom_ROOT,"nodered");fs.mkdirSync(data,{recursive:true});args.push("-p",`${resolved.port||1880}:1880`,"-v",`${data}:/data`,`-e`,`TZ=${resolved.timezone||process.env.TZ||"UTC"}`);if(resolved.credentialSecret)args.push("-e",`NODE_RED_CREDENTIAL_SECRET=${resolved.credentialSecret}`)}args.push(m.image);const r=await run("docker",args,{timeout:120000,maxBuffer:16*1024*1024});res.json({ok:true,id,container:m.container,output:r.stdout.trim()})}catch(e){res.status(500).json({ok:false,error:e.message,output:(e.stdout||"")+(e.stderr||"")})}});
app.post("/modules/:id/remove",async(req,res)=>{const id=String(req.params.id||""),m=MODULES[id];if(!m)return res.status(404).json({ok:false,error:"Unknown integration"});if(m.externalOnly)return res.status(409).json({ok:false,error:"Externally managed integrations are not removed from Classroom Control Hub."});try{if(await containerExists(m.container))await run("docker",["rm","-f",m.container],{timeout:30000});res.json({ok:true,id})}catch(e){res.status(500).json({ok:false,error:e.message})}});
const safeCommands={"docker-ps":["docker",["ps","-a"]],"docker-stats":["docker",["stats","--no-stream"]],"disk-usage":["df",["-h"]],"memory":["free",["-h"]],"network":["ip",["addr"]],"routes":["ip",["route"]],"dns":["cat",["/etc/resolv.conf"]],"compose-status":["docker",["compose","ps"]]};
app.post("/command",async(req,res)=>{try{const preset=String(req.body?.preset||"");if(!preset||!safeCommands[preset])return res.status(403).json({ok:false,error:"Only fixed diagnostic presets are supported"});const [cmd,args]=safeCommands[preset],r=await run(cmd,args,{timeout:30000,maxBuffer:16*1024*1024,cwd:preset==="compose-status"?HUB_ROOT:undefined});return res.json({ok:true,preset,output:(r.stdout||"")+(r.stderr||"")})}catch(e){res.status(500).json({ok:false,error:e.message,output:(e.stdout||"")+(e.stderr||"")})}});
const server=app.listen(PORT,"0.0.0.0",()=>console.log(`Classroom Control Hub Maintenance Agent listening on ${PORT}`));
let stopping=false;function stop(signal){if(stopping)return;stopping=true;console.log(`${signal} received; draining maintenance agent`);const force=setTimeout(()=>process.exit(1),10000);force.unref();server.close(()=>{clearTimeout(force);process.exit(0)})}
process.once("SIGTERM",()=>stop("SIGTERM"));process.once("SIGINT",()=>stop("SIGINT"));
